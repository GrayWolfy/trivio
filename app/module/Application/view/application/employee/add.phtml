<div class="container">
    <h1>Add Employee</h1>

    <?php
    $form = $this->form;
    $form->setAttribute('action', $this->url('employee', ['action' => 'add']));
    $form->prepare();
    ?>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <?= $this->form()->openTag($form) ?>

            <div class="form-group">
                <?= $this->formLabel($form->get('full_name'), 'Full Name') ?>
                <?= $this->formInput($form->get('full_name'), ['class' => 'form-control']) ?>
            </div>

            <div class="form-group text-center">
                <?= $this->formSubmit($form->get('submit'), ['class' => 'btn btn-primary']) ?>
            </div>

            <?= $this->form()->closeTag() ?>
        </div>
    </div>
</div>
